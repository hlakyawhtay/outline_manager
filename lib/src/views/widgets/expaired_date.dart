import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:outline_manager/src/model/access_keys.dart';

class ExpairedDate extends ConsumerWidget {
  final AccessKey accessKey;
  const ExpairedDate(this.accessKey, {super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}
